events {}

http {
    include mime.types;

    server {
        listen 80;
        listen 443;
        server_name ak.editorium.net www.ak.editorium.net;

        location / {
            proxy_pass http://ak_app:3000;
        }

        location /test {
            return 200 'Hello from Test';
        }
    }
}